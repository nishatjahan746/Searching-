def find_in(arr,target):
    indices = []
    for i in range (len(arr)):
        if arr[i] == target:
            indices.append(i)
    return indices

arr = [4, 2, 3, 2, 4, 2]
target = 2
result = find_in(arr,target)
print("output: " ,result)
        
    
